import styles from '../../styles/Home.module.css';

export default function Header(): JSX.Element {
  return (
    <>
      {/* <header className={styles.header}>&copy; StubbyDigits, {new Date().getFullYear()}</header> */}
    </>
  );
}
